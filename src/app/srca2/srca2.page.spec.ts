import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Srca2Page } from './srca2.page';

describe('Srca2Page', () => {
  let component: Srca2Page;
  let fixture: ComponentFixture<Srca2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Srca2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
